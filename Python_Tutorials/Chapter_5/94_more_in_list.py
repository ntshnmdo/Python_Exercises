# generate lists with range functions
numbers = list(range(1,11))
# print(numbers)

# something more about pop method
# numbers.pop() 
# print(numbers)
# therefore pop method, pop out the last element of the string/list.
# also it return the same element, for example
print(numbers.pop())

